# 测试用例

共48项；最新结果28通过、20失败、0阻塞。用例有顺序依赖，独立复现须按源快照准备合成数据。

## REG-001 注册角色QA_SHOP

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-001场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：增加1条账号，账号和角色数值准确

## REG-002 注册角色QA_CUSTOMER

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-002场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：增加1条账号，账号和角色数值准确

## REG-003 注册角色QA_RIDER

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-003场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：增加1条账号，账号和角色数值准确

## REG-004 注册角色QA_ADMIN

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-004场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：增加1条账号，账号和角色数值准确

## REG-005 重复账号

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-005场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝重复账号，数据不变

## REG-006 空用户名

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-006场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝空用户名

## REG-007 单引号用户名

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-007场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：合法姓名可以保存

## REG-008 非法角色255

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-008场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝非法角色

## DATA-001 手机号从数据库到UDP响应一致性

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中DATA-001场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：返回原始11位手机号13900000001

## AUTH-002 未登录读取账号列表的密码字段

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中AUTH-002场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：未认证请求不返回可用明文密码

## MENU-001 新增菜品价格25.5

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-001场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：新增菜品，金额准确

## MENU-002 菜品编辑价格

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-002场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：同商户菜单替换，价格26.75

## MENU-003 第二商户菜单不覆盖第一商户

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-003场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：保留第一商户菜品，新增第二商户菜品

## MENU-004 重复提交同商户菜单

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-004场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：不产生重复菜品

## MENU-005 负菜品价格

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-005场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：负价格拒绝且保留旧菜单

## MENU-006 单引号菜品名更新

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-006场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：合法名称可以更新，异常也不得删除原菜单

## MENU-007 恢复合成菜单

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-007场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：恢复合成菜单用于后续订单测试

## MENU-008 同批重复菜品UUID

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-008场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：同一UUID只保存1条菜品

## MENU-009 混合商户报文

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中MENU-009场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：混合批次不得制造重复菜品

## ORD-001 合法订单写入与金额字段保存

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-001场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：存储1条订单，金额25.5

## ORD-002 订单正常状态更新 1

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-002场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：仍1条订单，状态更新为1

## ORD-003 订单正常状态更新 2

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-003场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：仍1条订单，状态更新为2

## ORD-004 订单正常状态更新 3

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-004场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：仍1条订单，状态更新为3

## ORD-005 单条报文重复提交

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-005场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：相同订单重复发送不新增第二条

## ORD-006 已完成订单回退待接单

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-006场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：已完成订单不得回退到新建状态

## ORD-007 非法枚举状态255

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-007场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝非法状态，数据不变化

## ORD-008 负订单金额

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-008场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝负金额，数据不变化

## ORD-009 同批两条重复订单

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-009场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：同一业务订单只能保存1条

## ORD-010 合法名称包含单引号

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-010场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：合法菜品名称保留单引号并正常保存

## ORD-011 已存在订单更新为含单引号名称

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-011场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：更新失败时也不得丢失原订单

## NET-001 短报文

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-001场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：忽略短报文，数据库不变

## NET-002 未知消息类型

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-002场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：忽略未知报文，数据库不变

## NET-003 订单报文长度不足

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-003场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝不完整报文

## NET-004 订单报文多出1字节

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-004场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝长度不符报文

## ORD-012 六位整数金额的小数精度

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-012场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：123456.78金额精确保存

## AUTH-001 未配置客户端未登录提交订单

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中AUTH-001场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：未认证来源不能创建订单

## REG-009 中文用户名原样保存

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中REG-009场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：中文姓名完整保存

## ORD-013 中文菜品名称与小数金额

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-013场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：中文名称和0.01金额正确保存

## ORD-014 空订单UUID

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-014场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝空业务标识

## ORD-015 空读者对应的客户标识

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-015场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：订单必须关联客户，拒绝空客户标识

## ORD-016 空菜品名称

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-016场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝缺少菜品内容的订单

## ORD-017 常规金额9999.99

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ORD-017场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：9999.99原样保存

## NET-005 零字节数据报

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-005场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：忽略空数据报，状态不变

## NET-006 仅订单消息头

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-006场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：拒绝只有消息头的报文

## NET-007 空订单批次

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-007场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：空批次不删除或新增订单

## NET-008 空菜品批次

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中NET-008场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：空批次无法指定商户，不得删除其他菜单

## DATA-002 角色账号与中文姓名返回一致性

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中DATA-002场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：返回角色10至13，账号和中文用户名与数据库一致

## ENV-001 独立服务重启后的数据持久性

前置条件：独立Qt服务端已收到心跳；沿用本轮合成数据，精确前置快照见对应JSON

操作步骤：按测试脚本中ENV-001场景发送请求或重启；核对响应及SQLite前后数据。

预期结果：重启后表数据不丢失且用户列表可查询
